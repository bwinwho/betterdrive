/* Filled in at release-build time from GitHub repository secrets — see
   .github/workflows/release.yml. Stays empty in the committed source so
   nothing sensitive ever lands in git history. For local development, put
   your own values in a gitignored .env instead (see .env.example). */
module.exports = {
  GOOGLE_CLIENT_ID: '',
  GOOGLE_CLIENT_SECRET: '',
};
